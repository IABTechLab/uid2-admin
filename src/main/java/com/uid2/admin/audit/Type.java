package com.uid2.admin.audit;

/**
 * An enum of all table entities that uid2-admin handles.
 */

public enum Type {
    SITE,
    CLIENT,
    KEYACL,
    KEY,
    OPERATOR,
    ENCLAVE,
    PARTNER,
    ADMIN
}
